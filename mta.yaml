ID: helloworld
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: hello-node
   type: nodejs
   path: hello-node
   provides:
    - name: hello_node_api
      properties:
         url: ${default-url}
